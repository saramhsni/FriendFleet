

<div class="flex justify-center mb-5">
<nav class="user-nav w-11/12 rounded-bl-xl drop-shadow-lg shadow-[#337F5B]">
   
    <a href="{{path('app_micro_post')}}"><img src="{{asset('assets/images/Logo.png')}}" alt="logo" class="logo-nav " style="width: 80px;"></a>
    <ul id="menu-links">
        <li><a href="{{path('app_micro_post')}}">Home</a></li>
        <li><a href="{{path('app_profile', {id:app.user.id})}}">Profile</a></li>
        <li><a href="{{path('app_micro_post_add')}}">AddPost</a></li>
        <li><a href="{{path('app_messages')}}">Message <span class="border border-gray-2 px-2 py-1">{{ app.user.received.count}}</span></a></li>
    </ul>
    
    <div class="imgs-navbar">
    <span onclick="avatarMenu()">
    {{ include('shared/_avatar.html.twig',{profile: app.user.userProfile, size:12})}}
    </span>
    <div class="sub-menu-avatar" id="sub-menu-avatar">
        <div class="sub-menu-info">
            <div class="user-info">
                <div>
                    <h4 class="px-8 pb-2"><a href="{{path('app_profile', {id: app.user.id} )}}">{{ app.user.pseudo }}</a></h4>
                    <h4 class="px-8 pb-2"><a href="{{path('app_profile', {id: app.user.id} )}}">{{app.user.email}}</a></h4>
                </div>
                <hr class="hr-ligne">
                <h4 class="px-8 py-2"><a href="{{path('app_setting_profile', {id:app.user.id})}}">Setting Profile</a></h4>
                {% if 'ROLE_ADMIN' in app.user.roles %}
                <h4 class="px-8 py-2"><a href="{{path('app_admin')}}">Administration</a></h4>
                {% endif %}
                <h4 class="px-8 py-2"><a href="{{path('app_profile_deactivate', {id:app.user.id})}}">Deactive</a></h4>
                <h4 class="px-8 py-2"><a href="{{path('app_logout')}}">SignOut</a></h4>
            </div>
        </div>

    </div>
    <span onclick="toggleMenu()" class="img-menu" class="flex items-center">
    {# <img src="./images/menu.png" alt=""  > #}
    <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" class="w-12 h-12 ml-3">
      <path stroke-linecap="round" stroke-linejoin="round" d="M3.75 5.25h16.5m-16.5 4.5h16.5m-16.5 4.5h16.5m-16.5 4.5h16.5" />
    </svg>

    </span>
    </div>
   </nav> 
   <script src="{{ asset('assets/navbar.js') }}"></script>
</div>
<div>
        {% for message in app.flashes('success') %}
            <div style="color: green" class="mx-5 md:mx-15 xl:mx-20 mt-3">{{message}}</div>
        {% endfor %}
        {% block body %}
        {% endblock %}
</div>

