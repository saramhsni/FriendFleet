{% extends 'base.html.twig' %}

{% block title %}Create new profile{% endblock %}

{% block body %}
    {{ form_start(form) }}
        {{include ('micro_post/_menu.html.twig')}}
        {{include ('setting_profile/_setting_profile_tabs.html.twig')}}
        {{include ('setting_profile/_profile_form.html.twig')}}

        <div class="flex justify-center">
            <button type="submit" class="block w-4/12 shadow-sm border-transparent bg-[#00a7ff] hover:bg-[#0093ff] text-white rounded-md border p-2 mx-10 mt-4 mb-2">Save Setting</button>
        </div>
    {{ form_end(form) }}
{% endblock %}

