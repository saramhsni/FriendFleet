<form method="post" action="{{ path('app_admin_micro_post_delete', {'id': micro_post.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ micro_post.id) }}">
    <button class="btn">Delete</button>
</form>
